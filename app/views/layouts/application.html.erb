<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "TailView UI" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=<%= ENV.fetch("ANALYTICS_GA4_ID", "G-1RL0K1MKD2") %>"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', <%= ENV.fetch("ANALYTICS_GA4_ID", "G-1RL0K1MKD2") %>);
    </script>


    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Load Tailwind build and application stylesheet %>
    <%= stylesheet_link_tag "tailwind", "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body data-controller="smooth-scroll" class="<%= content_for?(:body_class) ? content_for(:body_class) : "" %>">

    <!-- Fixed notification areas -->
    <!-- Top Right -->
    <div id="notifications-top-right" class="fixed top-4 right-4 z-50 w-full max-w-md space-y-3 pointer-events-auto">
      <!-- Top-right notifications will be appended here -->
    </div>

    <!-- Top Left -->
    <div id="notifications-top-left" class="fixed top-4 left-4 z-50 w-full max-w-md space-y-3 pointer-events-auto">
      <!-- Top-left notifications will be appended here -->
    </div>

    <!-- Top Center -->
    <div id="notifications-top-center" class="fixed top-4 left-1/2 -translate-x-1/2 z-50 w-full max-w-md space-y-3 pointer-events-auto">
      <!-- Top-center notifications will be appended here -->
    </div>

    <!-- Bottom Right -->
    <div id="notifications-bottom-right" class="fixed bottom-4 right-4 z-50 w-full max-w-md space-y-3 pointer-events-auto">
      <!-- Bottom-right notifications will be appended here -->
    </div>

    <main class="<%= content_for?(:layout_main_class) ? content_for(:layout_main_class) : "container mx-auto px-5" %>">
      <%= yield %>
    </main>
  </body>
</html>
